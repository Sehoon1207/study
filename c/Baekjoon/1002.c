/*
문제
조규현과 백승환은 터렛에 근무하는 직원이다. 하지만 워낙 존재감이 없어서 인구수는 차지하지 않는다. 다음은 조규현과 백승환의 사진이다.
이석원은 조규현과 백승환에게 상대편 마린(류재명)의 위치를 계산하라는 명령을 내렸다. 조규현과 백승환은 각각 자신의 터렛 위치에서 현재 적까지의 거리를 계산했다.

조규현의 좌표 
$(x_1, y_1)$와 백승환의 좌표 
$(x_2, y_2)$가 주어지고, 조규현이 계산한 류재명과의 거리 
$r_1$과 백승환이 계산한 류재명과의 거리 
$r_2$가 주어졌을 때, 류재명이 있을 수 있는 좌표의 수를 출력하는 프로그램을 작성하시오.

입력
첫째 줄에 테스트 케이스의 개수 
$T$가 주어진다. 각 테스트 케이스는 다음과 같이 이루어져 있다.

한 줄에 공백으로 구분 된 여섯 정수 
$x_1$, 
$y_1$, 
$r_1$, 
$x_2$, 
$y_2$, 
$r_2$가 주어진다.

출력
각 테스트 케이스마다 류재명이 있을 수 있는 위치의 수를 출력한다. 만약 류재명이 있을 수 있는 위치의 개수가 무한대일 경우에는 
$-1$ 출력한다.

제한
 
-10,000 ≤ x_1, y_1, x_2, y_2 ≤ 10,000
 
1 ≤ r_1, r_2 ≤ 10,000

예제 입력 1 
3
0 0 13 40 0 37
0 0 3 0 7 4
1 1 1 1 1 5

예제 출력 1 
2
1
0

*/

#include <stdio.h>

int main() {
    int T;                                      // 테스트 개수
    if (scanf("%d", &T) != 1) return 0;         // 숫자 하나만 읽었는지 확인하는것

    while (T--) {                               // 테스트 한번 실행하면 T를 1씩 감소시킴
        long long x1, y1, r1, x2, y2, r2;
        scanf("%lld %lld %lld %lld %lld %lld", &x1, &y1, &r1, &x2, &y2, &r2);

        long long dx = x1 - x2;
        long long dy = y1 - y2;
        long long d2 = dx * dx + dy * dy;       // 중심 거리의 제곱
        long long sum = r1 + r2;
        long long sum2 = sum * sum;             // (r1 + r2)^2
        long long diff = r1 - r2;
        if (diff < 0) diff = -diff;
        long long diff2 = diff * diff;          // (|r1 - r2|)^2

        int ans;
        if (d2 == 0) {
            ans = (r1 == r2) ? -1 : 0;
        } else if (d2 > sum2) {
            ans = 0;                            // 서로 멀리 떨어짐
        } else if (d2 == sum2) {
            ans = 1;                            // 외접
        } else if (d2 < diff2) {
            ans = 0;                            // 한 원이 다른 원 내부 (비접)
        } else if (d2 == diff2) {
            ans = 1;                            // 내접
        } else {
            ans = 2;                            // 두 점에서 만남
        }

        printf("%d\n", ans);
    }
    return 0;

}