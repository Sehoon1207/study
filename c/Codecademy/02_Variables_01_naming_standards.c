#include<stdio.h>

int main() {
  
  int number;
  int test_value;
  int example;
  float int_count;
  printf("Hello World!\n");

/*
1. Names can be composed of upper and lower case letters, numbers, and underscores.
이름은 대문자와 소문자, 숫자, 밑줄로 구성
2. The first character must be a letter (upper or lower case).
첫 번째 문자는 문자(대문자 또는 소문자)여야 함
3. No keywords are allowed as the full name (int is not allowed but int_count would work).
전체 이름으로 키워드가 허용되지 x 
(int는 허용되지 않지만 int_count는 작동)
*/
}